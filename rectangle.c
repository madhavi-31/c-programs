// perimeter of rectangle 
#include<stdio.h>
void main()
{
    int l,b,per;
    printf("Enter the values of l,b:");
    scanf("%d%d",&l,&b);
    per=2*(l+b);
    printf("the perimeter of rectangle %d",per);
}
